<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Path to log file -->
    <!-- <property name="LOGS" value="./logs"/> -->

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <Pattern>
                %white(%d{HH:mm:ss}) %cyan(%-5t) %highlight(%-5level) %msg%n%throwable
            </Pattern>
        </encoder>
    </appender>

    <!--    <appender name="RollingFile"-->
    <!--              class="ch.qos.logback.core.rolling.RollingFileAppender">-->
    <!--        <file>${LOGS}/logger.log</file>-->
    <!--        <encoder-->
    <!--                class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">-->
    <!--            <charset>UTF-8</charset>-->
    <!--            <Pattern>%d %p %C{1.} [%t] %m%n</Pattern>-->
    <!--        </encoder>-->

    <!--        <rollingPolicy-->
    <!--                class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">-->
    <!--            &lt;!&ndash; rollover daily and when the file reaches 10 MegaBytes &ndash;&gt;-->
    <!--            <fileNamePattern>${LOGS}/archived/logger-%d{yyyy-MM-dd}.%i.log-->
    <!--            </fileNamePattern>-->
    <!--            <timeBasedFileNamingAndTriggeringPolicy-->
    <!--                    class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">-->
    <!--                <maxFileSize>10MB</maxFileSize>-->
    <!--            </timeBasedFileNamingAndTriggeringPolicy>-->
    <!--        </rollingPolicy>-->
    <!--    </appender>-->
    
    <!-- LOG everything at INFO or at "-Dlog.level=" level -->
    <root level="${log.level:-info}">
        <!--        <appender-ref ref="RollingFile"/>-->
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>